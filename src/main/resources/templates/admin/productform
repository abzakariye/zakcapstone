<!-- Create a new category of edit existing one) -->
<form th:action="@{/admin/products/save}" th:object="${product}" method="post">
    <input type="hidden" th:field="*{id}" />
    <label for="name">Name</label>
    <input type="text" th:field="*{name}" id="name" required />
    <br>

    <label for="description">Description</label>
    <textarea th:field="*{description}" id="description"></textarea>
    <br>

    <label for="price">Price</label>
    <input type="number" th:field="*{price}" id="price" required />
    <br>

    <label for="quantity">Quantity</label>
    <input type="number" th:field="*{quantity}" id="quantity" required />
    <br>

    <label for="productImage">Upload Product Image:</label>
    <input type="file" id="productImage" name="productImageUrl" />

    <br>

    <label for="isFeatured">Featured</label>
    <input type="checkbox" th:field="*{isFeatured}" name="isFeatured" id="isFeatured">
    <br>

    <button type="submit">Save Category</button>
    <a th:href="@{/}">Home page</a>
    <a th:href="@{/admin/products}">Products List</a>

</form>